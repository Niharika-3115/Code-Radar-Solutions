#include <stdio.h>

int main(){
    char character;
    scanf("%ch",&character);
    if (ch<='A' && ch>='Z'){
        printf("Uppercase");
    }
    else{
        printf("Lowercase");
    }
    return 0;
}